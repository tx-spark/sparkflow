sources:
  - name: raw_bills
    schema: tx_leg_raw_bills
    tables:
      - name: _legiscan_pulls
        description: Table containing information about pulls from Legiscan
        columns:
        - name: upload_time
          description: The time when the data was uploaded
          tests:
          - not_null
        - name: legiscan_hash
          description: Unique hash for the Legiscan pull
          tests:
          - not_null